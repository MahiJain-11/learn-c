#include <stdio.h>

int main()
{
	//Calculate sum of all elements in givn array

    // 	int ele[5]= {5,6,7,8,9};
    // 	int sum = 0;
    // 	for(int i=0; i<=4; i++) {
    // 		sum = sum + ele[i];
    // 	}
    // 	printf("sum of elements in array is %d",sum);
    
    //calculate the product of all the elements in array
    // int pro[3]={3,4,5};
    // int product = 1;
    // int size_of_array = sizeof(pro)/sizeof(pro[0]);// it tells the numb of 
    //elements
    // printf("%d",size_of_array);
    // for (int i =0 ; i<=2 ; i++){
    //     product = product *pro[i];
    // }
    // printf("product of the element is %d",product);
    
    // ***IMP find the maximum value out of all the elements in array
    // int arr[5]={10,4,13,23,45};
    // int max = -1;
    // for(int i = 0 ; i<=4 ; i++){
    //     if (max <arr[i]){
    //         max = arr[i];
    //     }
    // }
    // printf("%d\n",max);
    // NEGATIVE NUMBER
    //  int arr[5]={-10,-4,-13,-23,-45};
    // int max = arr[0];
    // for(int i = 0 ; i<=4 ; i++){
    //     if (max <arr[i]){
    //         max = arr[i];
    //     }
    // }
    // printf("%d\n",max);
    
    //Print the minimum value out of all the elements in array
    //  int arr[5]={1,41,133,45,29};
    // int min = arr[0];
    // for(int i = 0 ; i<=4 ; i++){
    //     if (min >arr[i]){
    //         min = arr[i];
    //     }
    // }
    // printf("%d",min);
    
    // -VE numbers
    // int arr[5]={-1,-41,-133,-45,-29};
    // int min = arr[0];
    // for(int i = 0 ; i<=4 ; i++){
    //     if (min >arr[i]){
    //         min = arr[i];
    //     }
    // }
    // printf("%d",min);
	return 0;
}