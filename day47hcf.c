#include <stdio.h>
    // int min(int a,int b) {
    // 	if(a>b) return a;
    // 	else return b;
    // }
    // int hcf(int a, int b) {
    // 	int gcd;
    // 	for (int i=1; i <= min(a,b); i++) {
    // 		if ( a%i == 0 && b%i == 0) {
    // 			gcd = i;
    // 		}
    // 	}
    // 	return gcd;
    // }
    
    // int factorial (int x){
    //     int f = 1;
    //     for(int i = 1; i <= x ; i++){
    //         f =f*i;
    //     }
    //     return f;
    // }
    
    // fibonacci number 
    // int fibbo (int x){
    //     int f = 1;
    //     for (int i = 1 ; i<=x ; i++){
    //         f= 
    //     }
        
    // }
    int main()
    {
    	// write a function to compute HCF oftwo given numbers
    	//METHOD 1:
        // 	int a;
        // 	printf("Enter a:\n");
        // 	scanf("%d",&a);
        // 	int b;
        // 	printf("Enter b:\n");
        // 	scanf("%d",&b);
        // 	int gcd = hcf(a,b);
        // 	printf("The HCF of %d and %d is %d", a,b, gcd);
    
    
        // print the factorial of 1st n numbers
        // int n;
        // printf("enter n\n");
        // scanf("%d" ,&n);
        // printf("%d",factorial(n));
        
        // print first n fibonacci numbers// it is the series in which  the next number 
        // is sum of previous 2 numbers
        //METHOD 1 USING FUNCTIONS
        
        
    	return 0;
    }
