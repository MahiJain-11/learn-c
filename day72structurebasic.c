#include <stdio.h>
struct student{
    char name[30];
    int age;
    float marks;
};

int main()
{
    //STRUCTURESS
    //structure (struct) is used when we want to store
    //different types of data together under one name.
    //like if we want int char and float together.
    //syntax:-
    //struct structure_name {
    //     data_type member1;
    //     data_type member2;
    //     data_type member3;
    // };
    
    // int main() {
    //     struct structure_name variable_name;
    // }
    // struct student s1 ={"Mahi",18,45.7};
    // printf("Name = %s\n", s1.name);
    // printf("Age = %d\n", s1.age);
    // printf("Marks = %.2f\n", s1.marks);
    
    
    return 0;
}