#include <stdio.h>

int main()
{ //POINTERS :- 
    // float price = 55.8;
    // float *ptr = &price; // shows value stored in age variable (dabba)
    // float _price = *ptr;// 
    //value stored in pointer variable 
    // printf("%.2f",_price);
    
    
    // int _age = *ptr;
    // int age = 22;
    // int *ptr = &age;
    // printf("%d",*ptr);
    // printf("%d",*(&age));
    // printf("%d",_age);
    // printf("%p",_age);// hexadecimal address
    // printf("%u", ptr); // %u se hum hexa decimal ko normal int mai likhs skte
    
    // char  star = '*';
    // char *ptr = &star;
    // char _star = *ptr;
    // printf("%c",_star);
    
    //
    return 0;
}